<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qf.jedis.dao.SiteDao">
<!--    List<Student> findAll();-->
    <select id="findAll" resultType="com.qf.jedis.entity.Site">
       select site_id,site_name,user_id from tb_site;
    </select>

</mapper>
